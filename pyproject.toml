[project]
name = "velodata-discord"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.6",
    "py-cord>=2.6.1",
    "python-dotenv>=1.0.1",
]
